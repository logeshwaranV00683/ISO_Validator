# Raw Message Validator — Frontend

React 19 + Vite + React Router v6 + Axios

## Stack
- **React 19** — UI
- **react-router-dom v6** — client-side routing
- **Axios** — API calls with JWT interceptors
- **Vite** — dev server with proxy to Spring Gateway

## Folder Structure